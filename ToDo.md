# yw-look ToDo

## 0. 進め方

- まずは `Tauri + React + Vite + Three.js` の最小骨格を作る
- MVP に直結する機能を優先し、後回し項目は混ぜない
- 速度計測とメモリ解放は初期から組み込む
- 1 つずつ動く状態を保ちながら積み上げる

## 1. プロジェクト初期セットアップ

- [ ] `Tauri` プロジェクトを初期化する
- [ ] `React + Vite + TypeScript` のフロント構成を作る
- [ ] `Three.js` と必要な loader 群を導入する
- [ ] 開発用の lint / format / typecheck を整備する
- [ ] ディレクトリ構成を決める
- [ ] フロントと Tauri 側の責務分離を決める
- [ ] ローカル設定保存先の方針を実装に落とす
- [ ] サンプルアセット置き場を決める

## 2. アプリ骨格

- [ ] 単一ウィンドウの基本レイアウトを作る
- [ ] 中央ビューア領域を作る
- [ ] 最小ツールバーを作る
- [ ] 下部ステータス領域を作る
- [ ] 空状態 UI を作る
- [ ] ローディング状態 UI を作る
- [ ] 代表的なエラー画面 UI を作る

## 3. ファイル入出力と起動導線

- [ ] ファイルダイアログから単一ファイルを開けるようにする
- [ ] Drag & Drop で開けるようにする
- [ ] 起動引数からファイルを受け取れるようにする
- [ ] 関連付け起動に必要な受け口を作る
- [ ] 現在ファイルのパス / 拡張子 / 種別を状態管理できるようにする
- [ ] フォルダ内の対応ファイル列挙処理を作る
- [ ] `Left Arrow / Right Arrow` で前後移動できるようにする
- [ ] 読み込み失敗時も前後移動を継続できるようにする

## 4. 3D ビューア基盤

- [ ] `Three.js` のレンダラー初期化を作る
- [ ] シーン / カメラ / ライト / 環境マップの初期化を作る
- [ ] 灰色ベース背景を作る
- [ ] 初期環境マップ 1 個を導入する
- [ ] `fit to view` を実装する
- [ ] 初期視点を斜め上に寄せる
- [ ] `Y-up` 基準で扱う実装を入れる
- [ ] 極端なスケールの警告判定を入れる
- [ ] カメラリセットを実装する

## 5. カメラ操作

- [ ] `OrbitControls` ベースを組み込む
- [ ] `Alt + 左ドラッグ = Orbit` を実装する
- [ ] `Alt + 中ドラッグ = Pan` を実装する
- [ ] `Alt + 右ドラッグ = Zoom` を実装する
- [ ] ホイールズームを実装する
- [ ] 操作対象がない時の入力ガードを入れる

## 6. 3D フォーマットローダー

- [ ] `glTF / GLB` ローダーを実装する
- [ ] `FBX` ローダーを実装する
- [ ] `OBJ` ローダーを実装する
- [ ] `USD` ローダーを experimental として実装する
- [ ] `PLY` ローダー対応可否を確認し、可能なら実装する
- [ ] `DAE` ローダー対応可否を確認し、可能なら実装する
- [ ] `STL` ローダー対応可否を確認し、可能なら実装する
- [ ] `VRM` 対応方針を確認し、可能なら実装する
- [ ] `PMD / PMX / VMD / VRMA` の対応方針を確認する
- [ ] 対応拡張子ごとの lazy load を実装する

## 7. テクスチャ / 画像ローダー

- [ ] `PNG / JPG / JPEG` 表示を実装する
- [ ] `TGA` 表示を実装する
- [ ] `HDR` 表示を実装する
- [ ] `EXR` 表示を実装する
- [ ] `DDS` 表示を実装する
- [ ] `KTX2` 表示対応可否を確認し、可能なら実装する
- [ ] `DDS` 読み込み失敗時のエラー分岐を整理する
- [ ] テクスチャ種別ごとの表示初期値を決める

## 8. 表示モード

- [ ] `テクスチャなし` モードを実装する
- [ ] `テクスチャあり` モードを実装する
- [ ] `ワイヤーフレーム` モードを実装する
- [ ] `テクスチャあり + ワイヤーフレーム` モードを実装する
- [ ] モード切替 UI を作る
- [ ] モード切替時のマテリアル差し替えと復元を安定化する

## 9. アニメーション UI

- [ ] アニメーション有無の判定を実装する
- [ ] アニメーション付きモデルだけバーを表示する
- [ ] 下部オーバーレイのアニメーションバーを作る
- [ ] `Play / Pause` を実装する
- [ ] シークバーを実装する
- [ ] フレーム送り / 戻しを実装する
- [ ] 時間表示を実装する
- [ ] 複数クリップ選択 UI を実装する

## 10. テクスチャ単体ビュー

- [ ] 画像ビューアと 3D ビューアの切り替え導線を整理する
- [ ] `RGB` 表示を実装する
- [ ] `RGBA` 表示を実装する
- [ ] `Alpha` 表示を実装する
- [ ] `HDR / EXR` のトーンマップ調整 UI を実装する
- [ ] 白飛び / 黒つぶれを確認しやすい表示調整を入れる
- [ ] 必要なら透過チェッカー表示を入れる

## 11. メタデータ / 階層 / 使用テクスチャ

- [ ] 基本メタデータ表示 UI を作る
- [ ] ファイル形式表示を実装する
- [ ] 形式バージョン表示を実装する
- [ ] ノード数 / メッシュ数などの簡易統計を実装する
- [ ] アニメーション有無表示を実装する
- [ ] 使用テクスチャ数表示を実装する
- [ ] 読み取り専用の階層ツリー表示を実装する
- [ ] 使用テクスチャ一覧 UI を実装する
- [ ] 埋め込み / 外部参照 / 未解決の区別表示を実装する
- [ ] 一覧から個別プレビューへ飛べるようにする

## 12. エラー表示と警告

- [ ] `未対応形式` エラー画面を作る
- [ ] `読み込み失敗` エラー画面を作る
- [ ] `参照解決失敗` エラー画面を作る
- [ ] `テクスチャ欠損` の警告表示を作る
- [ ] `スケール警告` の表示を作る
- [ ] エラー発生時の再読み込み導線を作る
- [ ] エラー詳細の内部ログ構造を決める

## 13. 最近開いたファイル

- [ ] 最近開いたファイルの保存形式を JSON で実装する
- [ ] 保存項目を `パス / 最終アクセス日時 / 種別` で実装する
- [ ] 最近開いたファイル一覧 UI を作る
- [ ] 存在しないパスのクリーンアップを入れる
- [ ] 件数上限を決めて実装する

## 14. パフォーマンスと安定性

- [ ] 読み込み時間の計測フックを入れる
- [ ] 起動時間の計測ポイントを入れる
- [ ] 前後移動時間の計測ポイントを入れる
- [ ] ローダーの lazy load を全対応拡張子で確認する
- [ ] ファイル切り替え時の `dispose` を徹底する
- [ ] `geometry / material / texture` 解放を点検する
- [ ] 重いファイル連続表示時のメモリ挙動を確認する
- [ ] EXR / HDR / DDS の重いケースを試験する

## 15. Windows 統合

- [ ] 関連付け対象拡張子の設定方法を決める
- [ ] インストーラーで関連付けを扱う方法を調べる
- [ ] 設定画面から関連付けオン / オフできる余地を作る
- [ ] 対象拡張子一覧を UI から参照できるようにする

## 16. ログと診断

- [ ] 内部ログの保存先を決める
- [ ] 開発中にログを確認しやすい導線を作る
- [ ] 最低限のエラーコード体系を決める
- [ ] パース失敗時の記録内容を決める

## 17. 将来対応の検討

- [ ] `Alembic (.abc)` の対応方針を調べる
- [ ] `ufbx` を使った native FBX パスの将来設計をまとめる
- [ ] `DDS` の native 展開が必要か検証する
- [ ] `USD` の参照解決強化の方針をまとめる
- [ ] サムネイルキャッシュが必要か再評価する

## 18. 最初の実装順

1. `Tauri + React + Vite + Three.js` の骨格を作る
2. ファイルを 1 つ開いて表示できる状態にする
3. `glTF / GLB` と画像表示を先に通す
4. カメラ操作と前後移動を入れる
5. メタデータ / 階層 / 使用テクスチャ一覧を入れる
6. `FBX / OBJ / DDS / EXR / HDR` を足す
7. 表示モードとアニメーション UI を入れる
8. エラー画面、最近開いたファイル、関連付けを整える
9. 計測とメモリ解放を詰める
